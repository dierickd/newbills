{% extends 'base.html.twig' %}

{% block title %}Edit FeatureProject{% endblock %}

{% block body %}
    <div class="main-header d-flex justify-content-between align-items-center">
        <h1 class="title">Editer la fonctionnalité {{ feature.name }}</h1>
    </div>


    <div class="container-main">
        {{ include('crud/feature/_form.html.twig', {
            'button_label': 'Mettre à jour',
            'action': path('app_feature_crud_edit', {'id': feature.id}),
            'form_type': 'new'
        }) }}

{#        <a href="{{ path('app_feature_crud_index') }}">back to list</a>#}

{#        {{ include('crud/feature/_delete_form.html.twig') }}#}
    </div>
{% endblock %}
