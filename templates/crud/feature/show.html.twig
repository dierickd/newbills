{% extends 'base.html.twig' %}

{% block title %}FeatureProject{% endblock %}

{% block body %}
    <h1>FeatureProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ feature_project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ feature_project.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_feature_project_crud_index') }}">back to list</a>

    <a href="{{ path('app_feature_project_crud_edit', {'id': feature_project.id}) }}">edit</a>

    {{ include('feature/_delete_form.html.twig') }}
{% endblock %}
