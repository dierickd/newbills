{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <div class="border-3 main-header d-flex justify-content-between align-items-center py-3">
        {% include '_components/_project_header.html.twig' %}
    </div>

    <section class="container">
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_category_crud_index') }}">back to list</a>

        <a href="{{ path('app_category_crud_edit', {'id': category.id}) }}">edit</a>

        {{ include('crud/category/_delete_form.html.twig') }}
    </section>
{% endblock %}
