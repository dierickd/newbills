{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
{#    <h1>Edit Project</h1>#}
    <turbo-frame id="{{ project.id }}">
        {{ include('project_crud/_form.html.twig', {
            'button_label': 'Mettre à jour',
            'action': path('app_project_crud_edit', {slug: project.slug}),
            'form_type': 'edit'
        }) }}

        {#        <a href="{{ path('app_project_crud_index') }}" class="btn d-inline-block ms-auto">Annuler</a> #}

        {#        {{ include('project_crud/_delete_form.html.twig') }} #}
    </turbo-frame>
{% endblock %}
