{% extends 'base.html.twig' %}

{% block title %}New Project{% endblock %}

{% block body %}
    <div class="main-header d-flex justify-content-between align-items-center">
        <h1 class="title">Création d'un nouveau projet</h1>
        {#        <a href="{{ path('app_project_crud_new') }}" class="btn btn-sm btn-primary d-inline-block">Créer un nouveau projet</a> #}
    </div>

    <div class="main-container">
        {{ include('project_crud/_form.html.twig', {
            'button_label': 'Créer le projet',
            'action': path('app_project_crud_new'),
            'form_type': 'new'
        }) }}
    </div>
    {#    <a href="{{ path('app_project_crud_index') }}">back to list</a> #}
{% endblock %}
